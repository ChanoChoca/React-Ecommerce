import CartWidget from "../CartWidget/CartWidget.jsx";

const NavBar = () => {
    return (
        <div>
            <h1>Navbar</h1>
            <CartWidget/>
        </div>
    );
};

export default NavBar;
